import { Button } from "@rneui/themed";
export default GenerateJoinButton = async ({props}) => {
  return (
    <Button>hi</Button>
  )
    // console.log("we made it here")
    // let account_id = await Storage.getItem({ key: `account_id` })
    // account_id = account_id.json();
    // console.log("from async storage, ", account_id)
    // if (account_id == props.leader_account_id) {
    //   return (
    //     <Button> Delete Event </Button>
    //   )
    // }

    // return (
    //   <Button>Join</Button>
    // )
}

